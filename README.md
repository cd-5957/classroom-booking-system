# 教室预约系统

基于云端的多人预约系统，支持每天各个时间段的预约占用管理。

## 功能特性

- 📅 时间段预约管理
- ⚡ 实时冲突检测
- 🔒 管理员权限控制
- ☁️ 云端数据库存储
- 🎨 响应式界面设计

## 技术栈

### 前端
- Vue 3 + Vite
- Tailwind CSS
- Axios

### 后端
- Express.js
- MongoDB (MongoDB Atlas 云端数据库)
- Mongoose

## 快速开始

### 1. 安装依赖

#### 前端依赖
```bash
npm install
```

#### 后端依赖
```bash
cd backend
npm install
```

### 2. 配置数据库

编辑 `backend/server.js` 文件，将 MongoDB Atlas 连接字符串替换为您自己的：

```javascript
mongoose.connect('mongodb+srv://your-username:your-password@cluster0.mongodb.net/classroom-booking?retryWrites=true&w=majority', {
  useNewUrlParser: true,
  useUnifiedTopology: true
});
```

### 3. 启动服务

#### 启动前端开发服务器
```bash
npm run dev
```

#### 启动后端服务器
```bash
cd backend
npm start
```

### 4. 访问系统

打开浏览器，访问 `http://localhost:3000` 即可使用预约系统。

## 使用说明

1. **预约占用**：在预约表单中填写编号和占用起止时间，点击提交预约。
2. **冲突检测**：系统会自动检测时间段冲突，避免重复预约。
3. **管理员操作**：仅管理员可以删除已有的预约记录。

## 项目结构

```
├── src/
│   ├── components/
│   │   ├── BookingForm.vue     # 预约表单组件
│   │   └── BookingList.vue     # 预约记录列表组件
│   ├── App.vue                 # 应用主组件
│   ├── main.js                 # 应用入口
│   └── style.css               # 全局样式
├── backend/
│   ├── models/
│   │   └── Booking.js          # 预约数据模型
│   ├── routes/
│   │   └── bookings.js         # 预约相关路由
│   ├── middleware/
│   │   └── auth.js             # 管理员验证中间件
│   ├── package.json            # 后端依赖配置
│   └── server.js               # 后端服务器入口
├── index.html                  # HTML 入口文件
├── package.json                # 前端依赖配置
├── vite.config.js              # Vite 配置
├── tailwind.config.js          # Tailwind CSS 配置
└── postcss.config.js           # PostCSS 配置
```